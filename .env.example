PORT=3000

FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccountKey.json

# Public base URL this server is reachable at, used to build each tenant's
# webhook URL when they connect. Update when the cloudflared tunnel URL
# changes, and again after moving to the VPS.
CLOUDINARY_CLOUD_NAME=vtdxdwve
CLOUDINARY_UPLOAD_PRESET=frph1y9b
